desc "send emails at 11am and 8pm every day"
task :send_emails do

  require_relative 'lm_hours.rb'
  if Time.now.hour > 8 and < 9 or Time.now.hour >20 and < 21
     Hours.get_hours_daily
     Hours.get_hours_weekly
     Hours.users_time
     Hours.send_email
 end
end